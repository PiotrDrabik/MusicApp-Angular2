import { FirstLargeLetterPipe } from './first-large-letter.pipe';

describe('FirstLargeLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLargeLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
